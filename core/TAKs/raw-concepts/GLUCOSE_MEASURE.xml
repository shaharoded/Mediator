<?xml version="1.0" encoding="UTF-8"?>
<raw-concept name="GLUCOSE_MEASURE" concept-type="raw-numeric">
    <categories>Measurements</categories>
    <description>Raw concept to manage the measurement of GLUCOSE</description>
    <attributes>
        <attribute name="GLUCOSE_LAB_MEASURE" type="numeric">
            <numeric-allowed-values>
                <allowed-value min="0" max="800"/>
            </numeric-allowed-values>
        </attribute>
        <attribute name="GLUCOSE_CAPILLARY_MEASURE" type="numeric">
            <numeric-allowed-values>
                <allowed-value min="0" max="700"/>
            </numeric-allowed-values>
        </attribute>
        <attribute name="GLUCOSE_MEASURE" type="numeric">
            <numeric-allowed-values>
                <allowed-value min="0" max="800"/>
            </numeric-allowed-values>
        </attribute>
    </attributes>

    <!-- Don't merge records (creating a tuple with a few values), treat all instances separately under one Concept-->
    <merge/>
    
    <!-- Emit value as (measure,) -->
    <tuple/>

</raw-concept>