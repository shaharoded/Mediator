<?xml version="1.0" encoding="UTF-8"?>
<pattern name="BMI_MEASURE_ON_ADMISSION" concept-type="local-pattern">
    <categories>Admission</categories>
    <description>Captures if BMI (Body Mass Index) was performed within reasonable time of admission. The data process place a BMI instance if was measured 6 month prior to admission.</description>
    <derived-from>
        <attribute name="BMI_MEASURE" tak="raw-concept"/>
        <attribute name="ADMISSION_EVENT" tak="event"/>
    </derived-from>
 
    <!-- Patterns can have unique or shared values -->
    <abstraction-rules order="first">
        <rule operator="and">
            <temporal-relation how='before' max-distance='48h'>
                <attribute name="ADMISSION_EVENT">
                    <allowed-value equal="True"/>
                </attribute>
                <attribute name="BMI_MEASURE" idx="0">
                    <allowed-value min="0"/>
                </attribute>
                <!-- OPTIONAL: Compliance function to define fuzzy time window -->
                <compliance-function>
                    <time-constraint-compliance trapezeA="0h" trapezeB="0h" trapezeC="48h" trapezeD="72h"/>
                </compliance-function>
            </temporal-relation>
        </rule>
    </abstraction-rules>
</pattern>