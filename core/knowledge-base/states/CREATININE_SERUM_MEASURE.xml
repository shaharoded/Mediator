<?xml version="1.0" encoding="UTF-8"?>
<state name="CREATININE_SERUM_MEASURE_STATE">
    <categories>Measurements</categories>
    <description>Abstraction for the measurements of CREATININE (Serum)</description>
    <derived-from name="CREATININE_SERUM_MEASURE" tak="raw-concept"/>

    <!-- Concatenate adjacent equal states within these windows; allow 1 outlier skip -->
    <persistence good-after="24h" interpolate="true" max-skip="1"/>

    <!-- Discretization rules by values in order of appearance in the tuple (idx) -->
    <!-- Range treated as min_value <= X < max_value -->
    <discretization-rules>
        <attribute idx="0">
            <rule value="Normal" min="0" max="1.3"/>
            <rule value="Mildly Elevated" min="1.3" max="2.5"/>
            <rule value="High" min="2.5" max="10"/>
            <rule value="Very High" min="10"/>
        </attribute>
        <!-- idx="1" (route) is nominal; no discretization needed -->
    </discretization-rules>
</state>