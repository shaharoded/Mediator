<?xml version="1.0" encoding="UTF-8"?>
<event name="MYOCARDIAL_INJURY_EVENT">
    <categories>Biomarkers</categories>
    <description>Myocardial injury event (e.g., myocardial infarction). Derived from troponin (not full on complication but important biomarker)</description>
    <derived-from>
        <attribute name="TROPONIN_MEASURE" tak="raw-concept" idx="0" ref="A1"/>
    </derived-from>
    
    <!-- 
    Abstraction rules: constraint types allowed in <allowed-value>:
    - equal="value"               → exact match
    - min="value"                 → >= value
    - max="value"                 → <= value
    - min="val1" max="val2"       → range [val1, val2]
    -->
    <abstraction-rules>
        <rule value="True" operator="or">
            <attribute ref="A1">
                <allowed-value min="600"/>  <!-- 1 time >= 600 -->
            </attribute>
        </rule>
    </abstraction-rules>
</event>