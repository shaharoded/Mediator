<?xml version="1.0" encoding="UTF-8"?>
<event name="SEVERE_HYPERGLYCEMIA_EVENT">
    <categories>Complications</categories>
    <description>Severe hyperglycemia event (extremely high glucose levels). Overlaps with regular hyperglycemia events, but is more severe (prediction target).</description>
    <derived-from>
        <attribute name="GLUCOSE_MEASURE" tak="raw-concept" idx="0" ref="A1"/>
    </derived-from>
    
    <!-- 
    Abstraction rules: constraint types allowed in <allowed-value>:
    - equal="value"               → exact match
    - min="value"                 → >= value
    - max="value"                 → <= value
    - min="val1" max="val2"       → range [val1, val2]
    -->
    <abstraction-rules>
        <rule value="True" operator="or">
            <attribute ref="A1">
                <allowed-value min="250"/>  <!-- 1 time >= 250 -->
            </attribute>
        </rule>
    </abstraction-rules>
</event>