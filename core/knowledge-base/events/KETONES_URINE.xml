<?xml version="1.0" encoding="UTF-8"?>
<event name="KETONES_URINE_MEASURE_EVENT">
    <categories>Measurements</categories>
    <description>Ketones measurement result as discrete non-temporal value</description>
    <derived-from>
        <attribute name="KETONES_URINE_MEASURE" tak="raw-concept" idx="0" ref="A1"/>
    </derived-from>
    
    <!-- 
    Abstraction rules: constraint types allowed in <allowed-value>:
    - equal="value"               → exact match
    - min="value"                 → >= value
    - max="value"                 → <= value
    - min="val1" max="val2"       → range [val1, val2]
    -->
    <abstraction-rules>
        <rule value="Negative" operator="and">
            <attribute ref="A1">
                <allowed-value max="0.0"/>
            </attribute>
        </rule>
        <rule value="Positive" operator="and">
            <attribute ref="A1">
                <allowed-value min="0.1"/>
            </attribute>
        </rule>
    </abstraction-rules>
</event>